{#{% extends "tease.twig" %}#}
{#{% block content %}#}
	{#<h2 class="h2"><a href="{{post.permalink}}">{{post.title}}</a></h2>#}
		{#<p>{{post.get_preview(25)}}</p>#}

		{#{% if post.get_thumbnail %}#}
			{#<img src="{{post.get_thumbnail.get_url}}" />#}
		{#{% endif %}#}
{#{% endblock %}#}

<a class='teaser-link' href="{{post.permalink}}">
    <article class="tease tease-{{post.post_type}}" id="tease-{{post.ID}}">
        {#{% block content %}#}

        <h2>{{post.title}}</h2>

        {% if post.get_thumbnail %}
            <img src="{{post.thumbnail.src|resize(150, 93)}}" />
        {% endif %}

        {#get_preview($len = 50, $force = false, $readmore = 'Read More', $strip = true)#}
        <p>{{post.get_preview(50,false,false,true)}}</p>
        {#<p>{{post.content|timber_get_preview }}</p>#}

        {#{% endblock %}#}
    </article>

</a>